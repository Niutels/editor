# `@landrush/runtime`

Landrush-owned game runtime and host adapters. This package may consume Pascal's public APIs, but Pascal packages must not import it.

The package owns player physics, world collider assembly, render telemetry, and presentation bridges that are specific to the multiplayer island experience.
